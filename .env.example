# Environment variables for tests (Vitest and Playwright)
# Copy this file to .env.local and fill in your values
# DO NOT commit .env.local to git (it should be in .gitignore)

# API authentication key for test requests
# Must match the API_KEY in apps/api-worker/.dev.vars
# Or use TEST_API_KEY for higher rate limits (recommended for tests)
API_KEY=your-api-key-here

# API base URL for tests
# Default: http://localhost:8787 (local API worker)
# Production: https://your-api-worker.workers.dev
API_BASE=http://localhost:8787

# Frontend URL for E2E tests (Playwright)
# Default: https://writeo.tre.systems/ (production)
# Local: http://localhost:3000
PLAYWRIGHT_BASE_URL=http://localhost:3000
