# Local development environment variables for Next.js web app
# Copy this file to .env.local and fill in your values
# DO NOT commit .env.local to git (it should be in .gitignore)

# API base URL for server-side calls
# Default: http://localhost:8787 (local API worker)
# Production: https://your-api-worker.workers.dev
API_BASE_URL=http://localhost:8787

# API authentication key
# Must match the API_KEY in apps/api-worker/.dev.vars
API_KEY=your-api-key-here

# Optional: API base URL for client-side calls (currently not used)
# All API calls go through Server Actions (server-side only)
NEXT_PUBLIC_API_BASE=http://localhost:8787
