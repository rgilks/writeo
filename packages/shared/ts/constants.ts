/**
 * Shared constants across frontend and backend
 */

/** Minimum number of words required for an essay */
export const MIN_ESSAY_WORDS = 250;

/** Maximum number of words allowed for an essay (soft cap - warns but allows) */
export const MAX_ESSAY_WORDS = 500;
